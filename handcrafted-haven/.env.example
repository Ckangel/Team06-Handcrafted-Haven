# ------------------------
# Database (Neon / Postgres)
# ------------------------
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DB?sslmode=require
DATABASE_URL_UNPOOLED=postgresql://USER:PASSWORD@HOST:PORT/DB?sslmode=require
PGHOST=HOST
PGHOST_UNPOOLED=HOST
PGUSER=USER
PGDATABASE=DB
PGPASSWORD=PASSWORD
POSTGRES_URL=postgresql://USER:PASSWORD@HOST:PORT/DB?sslmode=require
POSTGRES_URL_NON_POOLING=postgresql://USER:PASSWORD@HOST:PORT/DB?sslmode=require
POSTGRES_USER=USER
POSTGRES_HOST=HOST
POSTGRES_PASSWORD=PASSWORD
POSTGRES_DATABASE=DB
POSTGRES_URL_NO_SSL=postgresql://USER:PASSWORD@HOST:PORT/DB
POSTGRES_PRISMA_URL=postgresql://USER:PASSWORD@HOST:PORT/DB?connect_timeout=15&sslmode=require

# ------------------------
# NextAuth Configuration
# ------------------------
NEXTAUTH_URL=https://handcrafted-haven-sigma.vercel.app        # Production URL
NEXTAUTH_URL_INTERNAL=http://localhost:3000                    # Local development URL
NEXTAUTH_SECRET=YOUR_RANDOM_SECRET_HERE                        # 32+ chars, same for all devs
NEXT_PUBLIC_APP_URL=http://localhost:3000                      # Public URL for client-side redirects (set to production URL in Vercel)

# ------------------------
# GitHub OAuth
# ------------------------
GITHUB_CLIENT_ID=YOUR_GITHUB_CLIENT_ID
GITHUB_CLIENT_SECRET=YOUR_GITHUB_CLIENT_SECRET
GITHUB_REDIRECT_URI=https://handcrafted-haven-sigma.vercel.app/api/auth/callback/github
GITHUB_REDIRECT_URI_LOCAL=http://localhost:3000/api/auth/callback/github

# ------------------------
# Google OAuth
# ------------------------
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET=YOUR_GOOGLE_CLIENT_SECRET
GOOGLE_REDIRECT_URI=https://handcrafted-haven-sigma.vercel.app/api/auth/callback/google
GOOGLE_REDIRECT_URI_LOCAL=http://localhost:3000/api/auth/callback/google

# ------------------------
# UI / Auth (if using 3rd-party services)
# ------------------------
NEXT_PUBLIC_STACK_PROJECT_ID=
NEXT_PUBLIC_STACK_PUBLISHABLE_CLIENT_KEY=
STACK_SECRET_SERVER_KEY=
